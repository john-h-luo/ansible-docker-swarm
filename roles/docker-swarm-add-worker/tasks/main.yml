---
- name: Add Workers to the Swarm
  shell: "docker swarm join --token {{ hostvars['swarm-managers']['worker_token']['stdout'] }} {{ hostvars['swarm-managers']['ansible_default_ipv4']['address'] }}:2377"
  tags: swarm
